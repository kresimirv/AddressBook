package addressbook.operations;

import addressbook.AddressBookManager;

/**
 * AddressBookOperation interface
 *
 */
public interface AddressBookOperation {
	public void Execute(AddressBookManager addressBookManager);
}
